name: Release

on:
  push:
    branches: [ main ]
    tags: [ 'v*' ]

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
      # 1. Checkout repo
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          fetch-tags: true

      # 2. Install uv (tool manager for Python workflow)
      - name: Set up uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true
          version: "latest"

      # 3. Create virtual environment
      - name: Create virtual environment
        run: uv venv

      # 4. Install required tools
      - name: Install tools
        run: |
          uv pip install twine
          uv pip install python-semantic-release==7.34.6
          uv pip install build
      # 5. Semantic-release: compute version, commit, tag, GitHub release
      - name: Semantic release (version bump + commit + tag)
        working-directory: ${{ github.workspace }}
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "41898282+github-actions[bot]@users.noreply.github.com"

          uv run semantic-release publish \
            -v DEBUG \
            -D commit_author="github-actions[bot] <41898282+github-actions[bot]@users.noreply.github.com>"
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # 6. Build sdist with python -m build (after version bump)
      - name: Build sdist
        run: |
          uv run python -m build --sdist

      # 7. Upload sdist to GitHub release
      - name: Upload sdist to GitHub release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          TAG="$(git describe --tags --abbrev=0)"
          if ! gh release view "$TAG" >/dev/null 2>&1; then
            gh release create "$TAG" --notes ""
          fi
          gh release upload "$TAG" dist/*.tar.gz --clobber

  build-wheels:
    needs: release
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          fetch-tags: true

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install cibuildwheel
        run: |
          python -m pip install --upgrade pip
          python -m pip install cibuildwheel

      - name: Build wheels with cibuildwheel
        run: python -m cibuildwheel --output-dir dist

      - name: Upload wheels to GitHub release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        shell: bash
        run: |
          TAG="$(git describe --tags --abbrev=0)"
          if ! gh release view "$TAG" >/dev/null 2>&1; then
            gh release create "$TAG" --notes ""
          fi
          gh release upload "$TAG" dist/*.whl --clobber

  publish-pypi:
    needs: [release, build-wheels]
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          fetch-tags: true

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install twine
        run: |
          python -m pip install --upgrade pip
          python -m pip install twine

      - name: Download release artifacts
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          TAG="$(git describe --tags --abbrev=0)"
          if ! gh release view "$TAG" >/dev/null 2>&1; then
            echo "No GitHub release found for tag $TAG; nothing to download."
            exit 0
          fi
          mkdir -p dist
          gh release download "$TAG" --dir dist

      - name: Publish to PyPI
        env:
          TWINE_USERNAME: __token__
          TWINE_PASSWORD: ${{ secrets.PYPI_TOKEN }}
        run: |
          python -m twine upload dist/*
